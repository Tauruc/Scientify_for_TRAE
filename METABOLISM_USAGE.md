
# 知识代谢（心跳）功能使用说明

知识代谢功能可以让系统自动每日跟进领域最新研究进展，持续积累知识，自动生成研究假设。

## 快速开始

### 1. 初始化知识代谢

在 TRAE 聊天框输入：
```
/metabolism-init 关键词: "LLM efficiency, long context inference" 分类: cs.CL, cs.LG
```

系统会创建配置文件 `metabolism_config.json`，包含：
- 搜索关键词
- arXiv 分类（默认 cs.CV, cs.CL, cs.LG）
- 搜索来源（arXiv + OpenAlex）

### 2. 启动自动心跳

```
/metabolism-start
```

启动后系统会：
- 立即执行首次搜索
- 之后每24小时自动执行一次
- 每天只执行一次，重复启动不会重复执行

### 3. 停止心跳

```
/metabolism-stop
```

## 功能说明

### 每日自动执行的任务
1. **增量搜索**：自动搜索过去5天内的最新论文，去重避免重复下载
2. **论文下载**：优先下载tex源文件，回退到PDF
3. **知识沉淀**：自动为每篇新论文生成知识笔记，保存到 `knowledge/` 目录
4. **假设生成**：基于新增论文自动生成研究假设，保存到 `ideas/` 目录
5. **日志记录**：每日运行日志保存到 `logs/` 目录

### 生成的文件结构
```
your-research/
├── metabolism_config.json  # 代谢配置文件
├── papers/                 # 新下载的论文
├── knowledge/              # 自动生成的论文笔记
├── ideas/                  # 自动生成的研究假设
└── logs/                   # 每日运行日志
    └── YYYY-MM-DD.md
```

### 特点
- **断点续跑**：重启TRAE后重新运行 `/metabolism-start` 会自动从上次中断的位置继续
- **去重机制**：已处理过的论文不会重复下载和分析
- **防重复执行**：每天只会运行一次，避免重复工作
- **可配置**：可以随时修改 `metabolism_config.json` 调整关键词、分类等配置

## 手动触发

如果想立即手动执行一次心跳：
```
/metabolism-run
```

不需要等待定时任务，立即执行一次完整的每日更新。
